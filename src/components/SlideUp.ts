// Refined manually. Do not overwrite.

import { AfterViewInit, Component, ElementRef, OnDestroy, input, signal } from '@angular/core';
@Component({selector:'SlideUp',standalone:true,template:`<div class="motion" [class.is-visible]="visible()" [style.--duration.ms]="duration()" [style.--delay.ms]="delay()" [style.--offset.px]="offset()"><ng-content /></div>`,styles:[`:host{display:inline-block}.motion{opacity:0;transform:translateY(var(--offset))}.motion.is-visible{animation:slide-up var(--duration) cubic-bezier(.215,.61,.355,1) var(--delay) both}@keyframes slide-up{to{opacity:1;transform:translateY(0)}}`]})
export class SlideUp implements AfterViewInit,OnDestroy {duration=input(400);delay=input(0);offset=input(20);trigger=input(true);triggerOnVisibility=input(false);visibilityThreshold=input(.3);visible=signal(false);private observer?:IntersectionObserver;constructor(private el:ElementRef<HTMLElement>){}ngAfterViewInit(){if(this.triggerOnVisibility()){this.observer=new IntersectionObserver(([e])=>{if(e.isIntersecting){this.visible.set(true);this.observer?.disconnect()}},{threshold:this.visibilityThreshold()});this.observer.observe(this.el.nativeElement)}else this.visible.set(this.trigger())}ngOnDestroy(){this.observer?.disconnect()}}
